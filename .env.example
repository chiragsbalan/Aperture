# Shared local env template (expanded in P0.2+).
# Copy to .env and adjust. Never commit real secrets.

# Backend
APP_NAME=Aperture
APP_VERSION=0.1.0
ENVIRONMENT=local
LOG_LEVEL=INFO
API_V1_PREFIX=/api/v1
